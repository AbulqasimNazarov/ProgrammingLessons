#include <iostream>
using namespace std;


int pi = 3.14;

int main()
{
    
    int* a = &pi;
    cout << *a << endl;












    /*int num = 0;
    int u = 1;
    cout << "Enter number: ", cin >> num;

 

    for (int i = 1; i < 11; i++)
    {
        cout << num << " * " << i << " = " << num * i << endl;
    }*/

    /*int range = 0, capacity = 0;;
    char symbol = 42;
    cout << "Enter range: ";
    cin >> range;
    for (int i = 0; capacity <= range; i++)
    {
        for (int j = 0; j < capacity; j++)
        {
            cout << symbol;
        }
        cout << endl;
        capacity++;
    }
    for (int i = 0; range > 1; i++)
    {
        for (int j = 1; j < range; j++)
        {
            cout << symbol;
        }
        cout << endl;
        range--;
    }
    return 0;*/



}

